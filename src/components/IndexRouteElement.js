import styled from 'styled-components'

function IndexRouteElement() {
  return (
    <IndexRouteElementWrapper>
      <h2>Select a method for calculation</h2>
    </IndexRouteElementWrapper>
  )
}

const IndexRouteElementWrapper = styled.main`
  padding: 4rem;
  text-align: center;
`

export default IndexRouteElement
